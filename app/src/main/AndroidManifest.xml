<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aviation.recoding.zeppelin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/FullScreen">
        <activity android:name=".ui.StartUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.DashboardActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.ReceiptScanActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.flights.FlightInfoActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.leaderboard.GameActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.leaderboard.LeaderboardActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.flights.SelectionGallery"
            android:screenOrientation="portrait" />
        <activity android:name=".ui.HotSpotActivity"></activity>
    </application>

</manifest>